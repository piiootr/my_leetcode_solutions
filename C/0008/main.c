/*
0008
https://leetcode.com/problems/string-to-integer-atoi/
*/

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <limits.h>

int myAtoi(char* s) {
    int solution = 0;
    int s = 1;
    size_t i = 0;
    int c = 0;

    if(!s) return 0;

    while(i < strlen(s)) {
        i++;
    }

    return solution;
}

int main(int argc, char** argv)
{
    char s[] = "-123";
    printf("s: %s\n", s);
    printf("i: %d\n", d);

    return 0;
}